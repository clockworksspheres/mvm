fastmcp>=2.0.0
fastapi>=0.115.0
pydantic>=2.0.0
uvicorn[standard]>=0.30.0
psutil>=5.9.0
# Optional – only needed if you use the original mvm GUI elsewhere
# pyside6
# Windows-only helper used by some hypervisor checks
# pywin32; sys_platform == "win32"
